using DataTemplate.ViewModels;
using System.Windows;

namespace DataTemplate
{
    public partial class MainWindow : Window
    {
        //private readonly MainViewModel _viewModel;

        public MainWindow()
        {
            InitializeComponent();
            //_viewModel = new MainViewModel();
        }
    }
}
